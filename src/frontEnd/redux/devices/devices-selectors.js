export const selectDevices = state => state.devices.devices;

export const selectAddictionalDevices = state =>
  state.devices.addictionalDevices;

export const selectTypes = state => state.devices.types;

export const selectBrands = state => state.devices.brands;
